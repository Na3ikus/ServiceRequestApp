@using ServiceDeskSystem.Domain.Entities
@using ServiceDeskSystem.Application.Services.Localization
@inject ILocalizationService L

<!-- Assignment Actions Component -->
@if (ShowTakeButton || ShowReleaseButton)
{
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 p-6 border border-gray-100 dark:border-gray-700">
        <div class="flex items-center gap-3 mb-6">
            <div class="p-2 bg-blue-600 dark:bg-blue-700 rounded-lg">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
                </svg>
            </div>
            <h3 class="text-lg font-bold text-gray-800 dark:text-white">@L.Translate("details.assignment")</h3>
        </div>
        
        <div class="space-y-3 mt-4">
            @if (ShowTakeButton)
            {
                <button @onclick="OnTakeTicket" class="btn-take-ticket w-full flex items-center justify-center gap-2 px-6 py-3 bg-blue-600 dark:bg-blue-700 text-white rounded-lg hover:bg-blue-700 dark:hover:bg-blue-600 transition-all shadow-md hover:shadow-lg font-semibold">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                    </svg>
                    <span>@L.Translate("details.takeTicket")</span>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                    </svg>
                </button>
            }

            @if (ShowReleaseButton)
            {
                <button @onclick="OnReleaseTicket" class="btn-release-ticket w-full flex items-center justify-center gap-2 px-6 py-3 bg-red-600 dark:bg-red-700 text-white rounded-lg hover:bg-red-700 dark:hover:bg-red-600 transition-all shadow-md hover:shadow-lg font-semibold">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7a4 4 0 11-8 0 4 4 0 018 0zM9 14a6 6 0 00-6 6v1h12v-1a6 6 0 00-6-6zM21 12h-6"></path>
                    </svg>
                    <span>@L.Translate("details.releaseTicket")</span>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                    </svg>
                </button>
            }
        </div>
    </div>
}

@code {
    [Parameter]
    public Ticket? Ticket { get; set; }

    [Parameter]
    public int CurrentUserId { get; set; }

    [Parameter]
    public bool IsAdminOrDeveloper { get; set; }

    [Parameter]
    public bool IsAdmin { get; set; }

    [Parameter]
    public EventCallback OnTakeTicket { get; set; }

    [Parameter]
    public EventCallback OnReleaseTicket { get; set; }

    /// <summary>
    /// Show Take button if: ticket is free AND user is admin or developer.
    /// </summary>
    private bool ShowTakeButton =>
        Ticket?.DeveloperId is null && IsAdminOrDeveloper;

    /// <summary>
    /// Show Release button if: ticket is assigned to current user (admin can only release their own tickets).
    /// </summary>
    private bool ShowReleaseButton =>
        Ticket?.DeveloperId is not null &&
        Ticket.DeveloperId == CurrentUserId;
}

